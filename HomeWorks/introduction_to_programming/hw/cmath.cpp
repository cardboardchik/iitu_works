#include <iostream>
#include <cmath>

using namespace std;


int main(){
    /*
    int p, y;
    float const e = 2.7;
    float K;

    cin >> p;
    cin >> y;

    K = log(pow(p, 2) + pow(y, 3)) + pow(e, p);

    cout << K;
    */

    /*
    int n, y;
    float G;

    cin >> n;
    cin >> y;

    G = n * (y + 3.5) + sqrt(y);

    cout << G;
    */

    /*
    int a, t;
    float D;

    cin >> a;
    cin >> t;

    D = 9.8 * pow(a, 2) + 5.52 * cos(pow(t, 5));

    cout << D;
    */


    /*int x;
    float L;

    cin >> x;

    L = 1.51 * cos(pow(x, 2)) + 2 * pow(x, 3);

    cout << L;
    */

   /*
    int x, y;
    float const e = 2.7;
    float M;

    cin >> x;
    cin >> y;

    M = cos(2 * y) + 3.6 * pow(e, x);

    cout << M;
    */

   /*
    int m;
    float N;

    cin >> m;

    N = pow(m, 2) + 2.8* abs(m) + 0.55;

    cout << N;
    */

   /* 8
    int y;
    float T;

    cin >> y;

    T = sqrt(abs(6 * pow(y, 2) - 0.1 * y + 4));

    cout << T;
    */
   /*
    int x, y;
    float V;

    cin >> x;
    cin >> y;

    V = log(y + 0.95) + sin(pow(x, 4));

    cout << V;
    */

   /* //10
    int x, y, k;
    float const e = 2.7;
    float U;

    cin >> x;
    cin >> y;
    cin >> k;

    U = pow(e, y) + 7.355 * pow(k, 2) + pow(sin(x), 2);

    cout << U;
    */
    /* // 11
    int x, y;
    float S;

    cin >> x;
    cin >> y;

    S = 9.756 * pow(y, 7) + 2 * tan(x);

    cout << S;
    */
   /* // 12
    int x, t;
    float K;

    cin >> x;
    cin >> t;

    K = 7 * pow(t, 2) + 3 * sin(pow(x, 3)) + 9.2;

    cout << K;

    */

   // 13
    /*int y;
    float E;

    cin >> y;

    E = sqrt(abs(3 * pow(y, 2) + 0.5 * y + 4));

    cout << E;
    */

//    14
    /*
    int x, y;
    float const e = 2.7;
    float R;

    cin >> x;
    cin >> y;

    R = abs(sqrt(pow(sin(y), 2) + 6.835 + pow(e, x)));

    cout << R;
    */


//    15
/*
    int y;
    float H;

    cin >> y;

    H = sin(pow(y, 2) - 2.8 * y + sqrt(abs(y)));

    cout << H;

*/


// 16
    /*int y;
    float S;

    cin >> y;

    S = sqrt(cos(4 * pow(y, 2)) + 7.151);

    cout << S;
    */

//    17
    /*int y;
    float N;

    cin >> y;

    N = 3 * pow(y, 2) + sqrt(y + 1);

    cout << N;
    */

//    18
    /*int y;
    float Z;

    cin >> y;

    Z = 3 * pow(y, 2) + sqrt(pow(y, 3) + 1);

    cout << Z;
*/
// 19
    /*int y, n, g;
    float P;

    cin >> y;
    cin >> n;
    cin >> g;

    P  = n * sqrt(pow(y, 3) + 1.09 * g);

    cout << P;
    */

//    20
    /*int y, k, x;
    float const e = 2.7;
    float U;

    cin >> y;
    cin >> k;
    cin >> x;

    U = pow(e, k + y) + tan(x * sqrt(y));

    cout << U;
    */

//    21

    /*int y, h;
    float const e = 2.7;
    float P;

    cin >> y;
    cin >> h;

    P = pow(e, y + 5.5) + 9.1 * pow(h, 3);

    cout << P;
    */


//    22
    /*int y, x, u;
    float T;

    cin >> y;
    cin >> x;
    cin >> u;

    T  = sin(2 * u) * log(2 * pow(y, 2) + sqrt(x));

    cout << T;
    */

//    23
    /*int y, f;
    float const e = 2.7;
    float G;

    cin >> y;
    cin >> f;

    G = pow(e, 2*y) + sin(f);

    cout << G;
    */


    // 24
    /*int y;
    float F;

    cin >> y;

    F = 2 * sin(0.214 * pow(y, 5) + 1);

    cout << F;
    */

//    25
    /*int y, f;
    float const e = 2.7;
    float G;

    cin >> y;
    cin >> f;

    G =  pow(e, 2 * y) + sin(pow(f, 2));

    cout << G;
    */

//    26
    /*int p;
    float Z;

    cin >> p;

    Z = sin(pow(pow(p, 2) + 0.4), 3);

    cout << Z;
    */

//    27

    /*int x, y, v;
    float const e = 2.7;
    float W;

    cin >> x;
    cin >> y;
    cin >> v;

    W = 1.03 * v + pow(e, 2 * y) + tan(abs(x));

    cout << W;
    */

//    28
    /*int y, h;
    float const e = 2.7;
    float T;

    cin >> y;
    cin >> h;

    T = pow(e, y + h) + sqrt(abs(6.4 * y));

    cout << T;
    */


//    29
    /*int y;
    float N;

    cin >> y;

    N  = 3 * pow(y, 2) + sqrt(abs(y + 1));

    cout << N;
    */

//    30

    // int y, r;
    // float const e = 2.7;
    // float W;

    // cin >> y;
    // cin >> r;

    // W = pow(e, y + r) + 7.2 * sin(r);

    // cout << W;


    // 1
    // int y, f;
    // float const e  = 2.7;
    // float G;

    // cin >> y;
    // cin >> f;

    // G  = (pow(e, 2 * y) + sin(f)) / (log(3.8 * y + f));

    // cout << G;

    //2
    int y, d;
    float F;

    cin >> y;
    cin >> d;



    return 0;
}